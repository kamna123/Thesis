#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main()
{
  int x[5000UL];
  int y[20UL];
  int z[10UL];
  int f[20UL];
  int i;
  int a[500UL];
  int j;
  int k;
/*for(i=0;i<20;i++)
    {
        x[i]=i;
        y[i]=2*i;
    }*/
// PARALLELIZE IN CUDA START
  for (i = 1; i <= 20; i = (i + 1)) {
    a[i + 4] = (a[i + 4] + 10);
    y[i] = a[i + 6];
// a[i]=y[i+7];
//  a[i]=y[i+1];
  }
// PARALLELIZE IN CUDA END
/* for(i=1;i<46;i=i+1)
    {
     y[i+10]=a[i+2];
       x[i+4]=y[i+2];
       
     }*/
  return 0;
}
