#ifndef NEW_LOOP_NORMALIZATION
#define NEW_LOOP_NORMALIZATION
#include "ds.hpp"

//void setLoopLowerBound1(SgForStatement* forstmt, SgExpression* lb);
void copy_into_struct(std::string a, StringNode** temp);
void display( StringNode* head);
std::string store( StringNode* head);
void loop_normalization(SgNode* n,std::set <std::string> var);
#endif
